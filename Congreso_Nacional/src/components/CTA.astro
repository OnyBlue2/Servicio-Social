---
// laaas ctaa bro
---

<section class="cta">
  <div class="container">
    <div class="cta-content">
      <h2 class="cta-title">¿Listo para ser parte del futuro?</h2>
      <p class="cta-description">Únete a nosotros en esta experiencia única de aprendizaje y colaboración.</p>
      <a href="#contacto" class="btn-primary">Contáctanos</a>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 4rem 0;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    text-align: center;
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
    color: white;
  }

  .cta-title {
    font-size: 1.875rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .cta-description {
    font-size: 1.125rem;
    opacity: 0.9;
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .btn-primary {
    padding: 1rem 2rem;
    border-radius: 1rem;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: var(--transition-normal);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 160px;
    border: 2px solid transparent;
    background: white;
    color: var(--primary-color);
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
  }

  @media (max-width: 768px) {
    .cta-title {
      font-size: 1.5rem;
    }
  }
</style>